paths:
  /{api:api}:
    x-modules:
      - spec:
          components:
            schemas:
              second_parameter:
                description: Second parameter definition
          paths:
            /two:
              get:
                x-request-handler:
                  - return_nothing:
                      return:
                        status: 200
