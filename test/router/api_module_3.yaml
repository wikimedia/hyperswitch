paths:
  /{api:api}:
    x-modules:
      - spec:
          components:
            schemas:
              first_parameter: a string
          paths:
            /three:
              get:
                x-request-handler:
                  - return_nothing:
                      return:
                        status: 200
